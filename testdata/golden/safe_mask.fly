def login(password):
    mask password
    hashed = hash(password)
    return hashed

class _DB:
    def query(self, v):
        return ("q", v)

db = _DB()

def handle():
    safe uid
    clean = int(uid)
    return db.query(clean)
