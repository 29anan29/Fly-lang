##Python 安全增强关键字（共 8 个）

| 关键字 | 含义 | 防御目标 | 实现时机 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| **`safe`** | 强制净化污点变量 | SQL注入、命令注入、代码注入（`eval`） | **编译期**（污点追踪） | `safe user_input`<br>`clean = int(user_input)` |
| **`only`** | 白名单权限块 | 恶意/未授权模块调用（`os.system`、`subprocess`） | **编译期 + 运行时**（`__builtins__` 代理） | `only (json, math):`<br>`    ...` |
| **`lock`** | 锁定常量 + 防反射读取 | 全局常量篡改、敏感信息通过 `globals()` 泄露 | **编译期**（符号表拦截） | `lock SECRET = "abc"`<br>`# 任何修改/反射访问均报错` |
| **`mask`** | 遮蔽敏感数据 | 敏感信息（密码、token）通过日志/打印泄露 | **编译期**（输出上下文检测） | `mask password`<br>`# print(password) 报错` |
| **`cage`** | 限制资源（内存/CPU/时间） | 无限循环、大内存分配、递归炸弹（DoS） | **运行时**（资源监控装饰器） | `cage(max_time="5s", max_memory="100MB"):`<br>`    ...` |
| **`guard`** | 强制输入验证（类型、格式、范围） | 未校验外部输入导致逻辑错误或注入 | **编译期**（生成断言代码） | `guard age: int, 0 < age < 150` |
| **`seal`** | 冻结对象（禁止动态增删改属性） | 对象属性被篡改（如 `role` 从 `user` 改成 `admin`） | **编译期**（生成 `__setattr__` 拦截） | `seal class Admin:`<br>`    role = "admin"` |
| **`trace`** | 强制审计日志 | 关键操作（删除、转账）无记录，难以事后追溯 | **编译期**（插入日志调用） | `trace(level="INFO", args=True):`<br>`    def delete_user(uid): ...` |

---

## 各关键字完整示例

```fly
# ========== 1. safe ==========
from flask import request

def handle():
    uid = request.args.get('id')
    safe uid
    # 编译报错：未净化的 uid 传给 eval
    # data = eval(f"get_user({uid})")
    clean = int(uid)          # 清洗
    return db.query(clean)


# ========== 2. only ==========
only (json, math):
    def parse(raw):
        data = json.loads(raw)
        # 编译报错：os.system 不在白名单
        # os.system("rm -rf /")
        return math.sqrt(data['x'])


# ========== 3. lock ==========
lock SECRET_KEY = "abc-123"

def hack():
    # 编译报错：不能修改 lock 变量
    # SECRET_KEY = "new"
    # 编译报错：不能通过 globals() 读取
    # print(globals()['SECRET_KEY'])


# ========== 4. mask ==========
def login(password):
    mask password
    # 编译报错：不能打印或记录 mask 变量
    # print(f"密码: {password}")
    # logging.info(password)
    # 允许用于哈希比较
    hashed = hash(password)
    return hashed


# ========== 5. cage ==========
cage(max_time="2s", max_memory="50MB"):
    def heavy():
        data = [0] * 100_000_000   # 超内存则抛 ResourceExhaustedError
        while True: pass           # 超时则抛 TimeoutError


# ========== 6. guard ==========
def create_user(username, age):
    guard username: str, len(username) > 0, len(username) <= 20
    guard age: int, 0 < age < 150
    return User(username, age)


# ========== 7. seal ==========
seal class Admin:
    role = "admin"
    def __init__(self, name):
        self.name = name

admin = Admin("Alice")
# 编译报错：不能修改 seal 类实例的属性
# admin.role = "user"
# admin.permission = "all"


# ========== 8. trace ==========
trace(level="WARN", args=True, ret=True):
    def delete_user(uid):
        # 编译器自动插入：
        # logging.warning(f"delete_user called with uid={uid}")
        db.delete(uid)
        # 编译器自动插入：
        # logging.warning(f"delete_user returned None")
```

---

## 编译器实现总览

| 关键字 | 编译期动作 | 运行时残留 |
| :--- | :--- | :--- |
| `safe` | 构建数据流图，追踪污点变量传入危险函数（`eval`/`exec`/`os.system`/SQL拼接） | 无 |
| `only` | 替换代码块的 `__builtins__` 为白名单代理，并校验所有 `import` | 轻量级代理字典 |
| `lock` | 标记变量不可变，拦截所有 `Assign`、`AugAssign` 及反射函数（`globals`/`setattr`/`vars`） | 在 `__dict__` 中设只读描述符 |
| `mask` | 追踪变量，若出现在 `print`、`logging`、字符串格式化等输出上下文，报错 | 无 |
| `cage` | 包装函数，注入资源监控装饰器（使用 `resource` 模块或 `signal` 计时器） | 运行时监控器 |
| `guard` | 展开为 `if not` 断言，并在参数传递点插入检查 | 无（纯展开代码） |
| `seal` | 为类生成 `__setattr__` 和 `__delattr__`，禁止增删改属性 | 对象固定 |
| `trace` | 在函数入口/出口插入 `logging` 调用，并记录参数和返回值 | 无（展开代码） |

---

## 防御 OWASP Top 10 覆盖表

| OWASP 类别 | Spyre 关键字 |
| :--- | :--- |
| 注入（SQL/命令/代码） | `safe` |
| 失效访问控制 | `only` |
| 敏感数据泄露 | `mask`、`lock` |
| 安全配置错误 | `lock`（防止篡改） |
| 脆弱/过时组件 | `cage`（防止资源滥用） |
| 识别与认证失败 | `guard`（强制验证）、`trace`（审计） |
| 软件和数据完整性失败 | `seal`（防止对象篡改） |
| 日志与监控不足 | `trace` |

---